services:
  - type: web
    name: finae-store
    runtime: static
    buildCommand: echo done
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /api/*
        destination: https://finae-api.onrender.com/api/:splat
      - type: rewrite
        source: /*
        destination: /index.html
